<link rel="stylesheet" href="../public/css/header.min.css" />

<nav class="navbar navbar-expand-lg navbar-dark bg-color">
  <div class="container-fluid">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#ntoggle" aria-controls="ntoggle" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="ntoggle">
      <a class="navbar-brand" href="/">
        <img src="<%= _client.user.displayAvatarURL() %>" alt="<%= _client.user.username %>" width="30" height="30" class="d-inline-block align-top" />
        <b><%= _client.user.username %></b>
      </a>
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/commands?lang=<%- i18n.getLocale() %>">
            <%= i18n.__mf('website.commands') %></a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= _client.user.displayAvatarURL({ format: 'png', size: 4096 }); %>">
            <%= i18n.__mf('website.invite') %></a>
        </li>
      </ul>
      <div class="collapse navbar-collapse" id="navbarNavDarkDropdown">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <%= i18n.__mf('website.language.language'); %>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <a class="dropdown-item" onclick="changeLang('en')">
                <%= i18n.__mf('website.language.en'); %></a>
              <a class="dropdown-item" onclick="changeLang('fr')">
                <%= i18n.__mf('website.language.fr'); %></a>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </div>
  <form class="d-flex">
    <button class="btn btn-outline-success" type="button">
      <b><%= i18n.__mf('website.login'); %></b>
    </button>
  </form>
</nav>